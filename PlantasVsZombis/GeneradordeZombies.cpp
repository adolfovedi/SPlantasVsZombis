#include "GeneradordeZombies.h"

GeneradordeZombies::GeneradordeZombies(int tasa, int fila) : tasaGeneracion(tasa), filaGeneracion(fila) {
    srand(time(0));
}

void GeneradordeZombies::generarZombie() {
 
}